import React from 'react';
import s from './styles';
import { View, Text } from 'react-native';

const Register: React.FC = () => {
  return (
    <View style={s.root}>
      <Text style={s.root}>Settings</Text>
    </View>
  );
};

export default Register;
